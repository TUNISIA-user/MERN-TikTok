    // import mongoose from 'mongoose';
    // import User from './userModel.js'; // Import the User model if needed

    // const commentSchema = new mongoose.Schema({
    //     comments: [{
    //         text: { type: String },
    //         user: { type: mongoose.Schema.Types.ObjectId, ref: 'User' }
    //         // i think can be delte the user here and ref bcause when user sing in my tiktok creata 
    //         // varible name token and in this variable get  idboject of user and mabe get his name 
    //     }],
            
    // }, { timestamps: true });

    // const Comment = mongoose.model('Comment', commentSchema);

    // export default Comment;
